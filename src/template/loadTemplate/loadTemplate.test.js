/* @flow */
/**
 * loadTemplate.test.js
 * Written by: Connor Taylor
 */

describe('loadTemplate', () => {
  it('loads templates from the GTL', () => {

  });

  it('uses a cyto.config.js file to determine dependency files to load', () => {

  });
});
