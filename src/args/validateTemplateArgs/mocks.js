const NOT_OBJECT = [
  1
];

const MISSING_ID = [
  { foo: 'bar' }
];

const INVALID_TYPE = [
  {
    id: 'foo',
    type: 'invalid'
  }
];

export default {
  NOT_OBJECT,
  MISSING_ID,
  INVALID_TYPE,
};
