/* @flow */
/**
 * getArgsForTemplate.test.js
 * Written by: Connor Taylor
 */

describe('getArgsForTemplate', () => {
  it('does not prompt if the arg was already supplied', () => {

  });

  it('does not prompt if dontPrompt is set to true', () => {

  });

  it('calls promptForArg when an arg needs a value from the user', () => {

  });

  it('calls parseListArg when a user supplies a list arg value', () => {

  });

  it('process each argument one at a time, not in parallel', () => {

  });
});
