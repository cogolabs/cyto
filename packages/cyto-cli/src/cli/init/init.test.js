/* @flow */
/**
 * init.test.js
 * Written by: Connor Taylor
 */

describe('init', () => {
  it('creates a .cyto directory in the users home directory', () => {

  });

  it('prompts the user for their name and the path to the GTL', () => {

  });

  it('calls writeTemplate to output the new template to the filesystem', () => {

  });
});
